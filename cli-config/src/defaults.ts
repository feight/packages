

export const defaults = {
    local: {
        emulators: {
            firestore: {
                host: "127.0.0.1",
                port: 8081
            }
        },
        python: {
            server: {
                port: 8080
            }
        }
    }
};
